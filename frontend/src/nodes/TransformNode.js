import React from "react";
import TransformNodeOrganism from "../components/organisms/TransformNodeOrganism";

const TransformNode = ({ id, data }) => {
  return <TransformNodeOrganism id={id} data={data} />;
};

export default TransformNode;
