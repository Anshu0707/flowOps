import React from "react";
import FilterNodeOrganism from "../components/organisms/FilterNodeOrganism";

const FilterNode = ({ id, data }) => {
  return <FilterNodeOrganism id={id} data={data} />;
};

export default FilterNode;
