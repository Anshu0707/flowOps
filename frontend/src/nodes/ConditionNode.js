import React from "react";
import ConditionNodeOrganism from "../components/organisms/ConditionNodeOrganism";

const ConditionNode = ({ id, data }) => {
  return <ConditionNodeOrganism id={id} data={data} />;
};

export default ConditionNode;
