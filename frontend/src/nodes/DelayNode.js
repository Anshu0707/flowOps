import React from "react";
import DelayNodeOrganism from "../components/organisms/DelayNodeOrganism";

const DelayNode = ({ id, data }) => {
  return <DelayNodeOrganism id={id} data={data} />;
};

export default DelayNode;
